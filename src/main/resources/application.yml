spring:
  datasource:
    url: jdbc:h2:file:./testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
    open-in-view: false
    defer-datasource-initialization: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher

springdoc:
  swagger-ui:
    csrf:
      enabled: true

server:
  tomcat:
    accept-count: 1000
    max-connections: 10000
#    threads:
#      max: 1000

logging:
  level:
    root: DEBUG

hibernate:
  dialect: "org.hibernate.dialect.H2Dialect"

zielinskin:
  enable-scheduling: false
  cross-instance:
    external-instance-ip: 192.168.1.4
    number-of-object-instances: 15000